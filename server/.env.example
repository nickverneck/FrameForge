# FrameForge Server Configuration
# Copy this file to .env and fill in your API keys

# Google Gemini API Key
# Required for using Google Gemini AI models
# Get your key at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# Alternative Google API Key (for backwards compatibility)
# Either GOOGLE_API_KEY or GEMINI_API_KEY can be used
GEMINI_API_KEY=your_google_api_key_here

# Fal.ai API Key
# Required for using Fal.ai image generation models (flux, etc.)
# Get your key at: https://fal.ai/dashboard/keys
FAL_KEY=your_fal_api_key_here

# Google Model ID
# Specifies which Google Gemini model to use
# Default: gemini-2.5-flash-image-preview
# Other options: gemini-2.0-flash-exp, gemini-exp-1206
GOOGLE_MODEL_ID=gemini-2.5-flash-image-preview

# CORS Allowed Origins
# Comma-separated list of origins allowed to access the API
# Default if not set: ["*"] (allows all origins - development only!)
# Recommended: Explicitly set allowed origins for security
# Development: Use localhost URLs
# Production: Specify your frontend domain only
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Server Host
# The IP address to bind the server to
# 0.0.0.0 allows connections from any network interface
# 127.0.0.1 restricts to localhost only
HOST=0.0.0.0

# Server Port
# The port number the server will listen on
# Default: 8000
PORT=8000
